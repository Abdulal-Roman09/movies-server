# Movie Management API

**Movie Management API** হলো একটি সাধারণ RESTful API যা Node.js, Express, MongoDB, Mongoose, TypeScript, এবং Zod ব্যবহার করে তৈরি। এটি মুভি ডাটা সংরক্ষণ, ভ্যালিডেশন, এবং CRUD অপারেশনের জন্য ব্যবহৃত হয়। এই প্রজেক্টটি শিক্ষানবিশদের জন্য আদর্শ, কারণ এটি সহজ এবং প্রতিটি লাইন বাংলায় ব্যাখ্যা করা আছে।

## 📋 প্রজেক্ট ওভারভিউ
- **উদ্দেশ্য**: মুভির তথ্য (টাইটেল, ডেসক্রিপশন, রিলিজ ডেট, জনরা, রিভিউ) সংরক্ষণ ও ম্যানেজ করা।
- **টেকনোলজি**:
  - Node.js: সার্ভার রান করার জন্য।
  - Express: API রাউটিং।
  - MongoDB: NoSQL ডাটাবেস।
  - Mongoose: MongoDB-এর জন্য ORM।
  - TypeScript: টাইপ সেফটি।
  - Zod: ডাটা ভ্যালিডেশন।
- **ফিচার**:
  - মুভি তৈরি (POST /api/movies)।
  - ডাটা ভ্যালিডেশন।
  - MongoDB-তে ডাটা সেভ।

## 📁 ফোল্ডার স্ট্রাকচার
movie-management-api/
├── src/
│   ├── modules/
│   │   └── movies/
│   │       ├── movies.controller.ts  # রিকোয়েস্ট হ্যান্ডলিং
│   │       ├── movies.interface.ts   # TypeScript টাইপ ডেফিনিশন
│   │       ├── movies.model.ts       # MongoDB স্কিমা
│   │       ├── movies.routes.ts      # API রাউট
│   │       ├── movies.service.ts     # ডাটাবেস অপারেশন
│   │       └── movies.validation.ts  # ডাটা ভ্যালিডেশন
│   ├── app.ts                       # Express অ্যাপ সেটআপ
│   └── server.ts                    # সার্ভার স্টার্ট
├── package.json                     # প্রজেক্ট ডিপেন্ডেন্সি
├── tsconfig.json                    # TypeScript কনফিগারেশন
└── README.md                        # প্রজেক্ট ডকুমেন্টেশন


## 🚀 ইন্সটলেশন ও সেটআপ
1. **রিপোজিটরি ক্লোন**:
   ```bash
   git clone <repository-url>
   cd movie-management-api
   ```

2. **ডিপেন্ডেন্সি ইন্সটল**:
   ```bash
   npm install
   ```

3. **MongoDB সংযোগ**:
   - MongoDB লোকালি ইন্সটল করুন অথবা MongoDB Atlas ব্যবহার করুন।
   - `server.ts` ফাইলে MongoDB URL আপডেট করুন।

4. **সার্ভার রান**:
   ```bash
   npm run start
   ```
   সার্ভার `http://localhost:5000` এ চলবে।

5. **API টেস্ট**:
   - **POST /api/movies** এর মাধ্যমে মুভি তৈরি করুন।
   - উদাহরণ রিকোয়েস্ট:
     ```json
     {
       "title": "Inception",
       "description": "A mind-bending thriller",
       "releaseDate": "2010-07-16",
       "genre": "Sci-Fi",
       "slug": "inception-2010",
       "reviews": [
         {
           "email": "user@example.com",
           "rating": 4,
           "comment": "Great movie!"
         }
       ]
     }
     ```

## 🛠️ ডাটা ফ্লো
1. **ক্লায়েন্ট** POST `/api/movies` রিকোয়েস্ট পাঠায়।
2. **রাউটার** রিকোয়েস্টটি কন্ট্রোলারে পাঠায়।
3. **কন্ট্রোলার** Zod দিয়ে ডাটা ভ্যালিডেট করে।
4. **কন্ট্রোলার** সার্ভিসকে কল করে।
5. **সার্ভিস** Mongoose মডেলের মাধ্যমে MongoDB-তে ডাটা সেভ করে।
6. **ডাটাবেস** থেকে ফলাফল ফিরে আসে।
7. **কন্ট্রোলার** ক্লায়েন্টের কাছে রেসপন্স পাঠায়।

## 📊 ফ্লো ডায়াগ্রাম
Client
   |
   v
Router ---> Controller ---> Validation (Zod) ---> Service ---> Model (Mongoose) ---> MongoDB
   ^                                                                       |
   |                                                                       v
   ------------------------------- Response -------------------------------

---

### **Code Files**

### 1. movies.model.ts

```typescript
import { model, Schema } from "mongoose";
import { TMovie, TReviews } from "./movies.interface";

// রিভিউ সাব-স্কিমা: মুভির রিভিউগুলোর স্ট্রাকচার
const reviewSchema = new Schema<TReviews>({
  email: { type: String, required: true }, // ইউজারের ইমেইল, বাধ্যতামূলক
  rating: { type: Number, required: true }, // রেটিং (0-5), বাধ্যতামূলক
  comment: { type: String, required: true }, // কমেন্ট, বাধ্যতামূলক
});

// মুভি স্কিমা: মুভির সম্পূর্ণ স্ট্রাকচার
const movieSchema = new Schema<TMovie>({
  title: { type: String, required: true }, // মুভির নাম, বাধ্যতামূলক
  description: { type: String, required: true }, // মুভির বর্ণনা, বাধ্যতামূলক
  releaseDate: { type: String, required: true }, // রিলিজ ডেট (YYYY-MM-DD), বাধ্যতামূলক
  genre: { type: String, required: true }, // জনরা (যেমন: Sci-Fi), বাধ্যতামূলক
  slug: { type: String, required: true }, // ইউনিক আইডেন্টিফায়ার, বাধ্যতামূলক
  reviews: { type: [reviewSchema], default: [] }, // রিভিউ অ্যারে, ডিফল্ট খালি
  isDelete: { type: Boolean, default: false }, // ডিলিট স্ট্যাটাস, ডিফল্ট false
  viewCount: { type: Number, default: 0 }, // ভিউ কাউন্ট, ডিফল্ট 0
});

// Mongoose মডেল তৈরি: MongoDB-তে ডাটা সেভ করার জন্য
export const Movie = model<TMovie>("Movie", movieSchema);
লাইন-বাই-লাইন ব্যাখ্যা:

import { model, Schema }: Mongoose থেকে মডেল এবং স্কিমা ইমপোর্ট করা হয়েছে।

reviewSchema: রিভিউর স্ট্রাকচার ডিফাইন করে, যেমন ইমেইল, রেটিং, কমেন্ট।

movieSchema: মুভির সম্পূর্ণ স্ট্রাকচার ডিফাইন করে, যেমন টাইটেল, রিভিউ, ভিউ কাউন্ট।

model<TMovie>: MongoDB-তে "Movie" নামে একটি কালেকশন তৈরি করে।

2. movies.interface.ts
TypeScript

export type TReviews = {
  email: string; // ইউজারের ইমেইল
  rating: number; // রেটিং (0-5)
  comment: string; // কমেন্ট
};

export type TMovie = {
  title: string; // মুভির নাম
  description: string; // মুভির বর্ণনা
  releaseDate: string; // রিলিজ ডেট
  genre: string; // জনরা
  slug: string; // ইউনিক স্লাগ
  reviews?: TReviews[]; // রিভিউ অ্যারে, অপশনাল
  isDelete?: boolean; // ডিলিট স্ট্যাটাস, অপশনাল
  viewCount?: number; // ভিউ কাউন্ট, অপশনাল
};
লাইন-বাই-লাইন ব্যাখ্যা:

TReviews: রিভিউ ডাটার টাইপ ডিফাইন করে।

TMovie: মুভি ডাটার টাইপ ডিফাইন করে, যেখানে কিছু ফিল্ড অপশনাল।

?: অপশনাল ফিল্ড নির্দেশ করে, যা না থাকলেও কোড চলবে।

3. movies.validation.ts
TypeScript

import { z } from "zod";

// রিভিউ স্কিমা: রিভিউ ডাটা ভ্যালিডেশন
export const reviewSchema = z.object({
  email: z.string().email(), // ইমেইল ফরম্যাটে হতে হবে
  rating: z.number().min(0).max(5), // রেটিং 0-5 এর মধ্যে
  comment: z.string().min(1), // কমেন্ট খালি হতে পারবে না
});

// মুভি স্কিমা: মুভি ডাটা ভ্যালিডেশন
export const movieSchema = z.object({
  title: z.string().min(1), // টাইটেল খালি হতে পারবে না
  description: z.string().min(1), // ডেসক্রিপশন খালি হতে পারবে না
  releaseDate: z.string().min(1), // রিলিজ ডেট খালি হতে পারবে না
  genre: z.string().min(1), // জনরা খালি হতে পারবে না
  slug: z.string().min(1), // স্লাগ খালি হতে পারবে না
  reviews: z.array(reviewSchema).optional().default([]), // রিভিউ অপশনাল
  isDelete: z.boolean().default(false), // ডিলিট স্ট্যাটাস, ডিফল্ট false
  viewCount: z.number().min(0).default(0), // ভিউ কাউন্ট, ডিফল্ট 0
});
লাইন-বাই-লাইন ব্যাখ্যা:

z.object: Zod দিয়ে ডাটা স্ট্রাকচার ডিফাইন করে।

z.string().email(): ইমেইল ফরম্যাট চেক করে।

z.number().min(0).max(5): রেটিং 0-5 এর মধ্যে হতে হবে।

optional().default([]): রিভিউ না থাকলে খালি অ্যারে।

4. movies.routes.ts
TypeScript

import express from "express";
import { MovieController } from "./movies.controller";

// Express রাউটার তৈরি
const router = express.Router();

// POST রাউট: নতুন মুভি তৈরি
router.post("/", MovieController.createMovie);

// রাউটার এক্সপোর্ট
export const MovieRoutes = router;
লাইন-বাই-লাইন ব্যাখ্যা:

express.Router(): Express-এর রাউটার তৈরি করে।

router.post("/", ...): POST রিকোয়েস্ট হ্যান্ডল করার জন্য কন্ট্রোলার ফাংশন কল করে।

export: রাউটারকে অন্য ফাইলে ব্যবহারের জন্য এক্সপোর্ট করে।

5. movies.controller.ts
TypeScript

import { Request, Response } from "express";
import { movieSchema } from "./movies.validation";
import { MovieService } from "./movies.service";

// নতুন মুভি তৈরির ফাংশন
const createMovie = async (req: Request, res: Response) => {
  try {
    // ইউজার থেকে আসা ডাটা ভ্যালিডেট করা
    const movieData = movieSchema.parse(req.body);

    // সার্ভিসে ডাটা পাঠিয়ে ডাটাবেসে সেভ করা
    const result = await MovieService.createMovie(movieData);

    // সফল রেসপন্স পাঠানো
    res.status(201).json({
      success: true,
      data: result,
    });
  } catch (error: any) {
    // ভুল হলে এরর রেসপন্স পাঠানো
    res.status(400).json({
      success: false,
      message: error.message,
    });
  }
};

// কন্ট্রোলার এক্সপোর্ট করা
export const MovieController = { createMovie };
লাইন-বাই-লাইন ব্যাখ্যা:

import { Request, Response }: Express-এর রিকোয়েস্ট এবং রেসপন্স টাইপ ইমপোর্ট।

movieSchema.parse: Zod দিয়ে ডাটা ভ্যালিডেট করে।

MovieService.createMovie: সার্ভিসে ডাটা পাঠায় ডাটাবেসে সেভ করার জন্য।

res.status(201): সফল রেসপন্স 201 স্ট্যাটাস কোড সহ পাঠায়।

catch (error): ভ্যালিডেশন বা অন্য কোনো এরর হলে 400 স্ট্যাটাস পাঠায়।

6. movies.service.ts
TypeScript

import { TMovie } from "./movies.interface";
import { Movie } from "./movies.model";

// মুভি তৈরির ফাংশন
const createMovie = async (payload: TMovie) => {
  // Mongoose মডেল ব্যবহার করে ডাটাবেসে সেভ করা
  return await Movie.create(payload);
};

// সার্ভিস এক্সপোর্ট করা
export const MovieService = { createMovie };
লাইন-বাই-লাইন ব্যাখ্যা:

import { TMovie }: মুভির টাইপ ইমপোর্ট করে।

Movie.create: Mongoose মডেল ব্যবহার করে ডাটাবেসে ডাটা সেভ করে।

export: সার্ভিস ফাংশন অন্য ফাইলে ব্যবহারের জন্য এক্সপোর্ট করে।

7. app.ts
TypeScript

import express from "express";
import { MovieRoutes } from "./modules/movies/movies.routes";

// Express অ্যাপ তৈরি
const app = express();

// JSON রিকোয়েস্ট পার্স করার জন্য মিডলওয়্যার
app.use(express.json());

// মুভি রাউট যুক্ত করা
app.use("/api/movies", MovieRoutes);

// হোম রাউট
app.get("/", (req, res) => {
  res.send("Hello World!");
});

// অ্যাপ এক্সপোর্ট করা
export default app;
লাইন-বাই-লাইন ব্যাখ্যা:

express(): Express অ্যাপ তৈরি করে।

app.use(express.json()): JSON রিকোয়েস্ট পার্স করার জন্য মিডলওয়্যার।

app.use("/api/movies", MovieRoutes): মুভি রাউট যুক্ত করে।

app.get("/"): হোম রাউটে "Hello World!" পাঠায়।

8. server.ts
TypeScript

import mongoose from "mongoose";
import app from "./app";

// সার্ভার স্টার্ট করার ফাংশন
async function startServer() {
  // MongoDB-এর সাথে সংযোগ করা
  await mongoose.connect("mongodb://127.0.0.1:27017/moviesdb");
  console.log("Database connected");

  // Express সার্ভার 5000 পোর্টে চালু করা
  app.listen(5000, () => {
    console.log("Server running on port 5000");
  });
}

// ফাংশন কল করা
startServer();
লাইন-বাই-লাইন ব্যাখ্যা:

mongoose.connect: MongoDB-এর সাথে সংযোগ স্থাপন করে।

app.listen(5000): Express সার্ভার 5000 পোর্টে চালু করে।

startServer(): সার্ভার শুরু করার ফাংশন কল করে।

